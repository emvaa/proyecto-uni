import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler: true, // Deshabilitado por compatibilidad - puede causar errores con hooks complejos
};

export default nextConfig;
